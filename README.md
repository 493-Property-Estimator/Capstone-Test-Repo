# Group 14 Capstone Repo

## Data ingestion

Use `./ingest` from the repository root.

Examples:

```bash
./ingest list-sources
./ingest ingest
./ingest ingest --source-key geospatial.pois
```

Details:

- `src/data_sourcing/README.md`
- `docs/ingestion_sources.md`
