# Quickstart: UC-06 Property Details Estimate

This plan defines the refined estimate flow using property attributes. Implementation is not yet present in the repository.

## Expected Runtime Shape
- **Frontend**: static HTML/CSS/JS served from `frontend/`
- **Backend**: Python HTTP service in `backend/`

## When Implementation Exists
- Start backend service (Python 3.x) from `backend/`
- Serve frontend files from `frontend/` (static server)
- Submit a location with property attributes and review the refined estimate

## Tests
- Acceptance tests: `Acceptance Tests/UC-06-AT.md`
- Unit/integration tests to be added per constitution gates
