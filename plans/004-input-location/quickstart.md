# Quickstart: UC-04 Normalization

This plan defines the normalization pipeline for property inputs to canonical location IDs. Implementation is not yet present in the repository.

## Expected Runtime Shape
- **Backend**: Python normalization service in `backend/`

## When Implementation Exists
- Start backend service (Python 3.x) from `backend/`
- Submit normalization requests with address or coordinate inputs

## Tests
- Acceptance tests: `Acceptance Tests/UC-04-AT.md`
- Unit/integration tests to be added per constitution gates
